<h1>Editing Book</h1>

<%= form_for(@book) do |f| %>
  <%= f.label :title, "Title" %>
  <%= f.text_field :title %>
  <%= f.label :body, "Opinion" %>
  <%= f.text_area :body %>
  <%= f.submit "Update Book" %>
 <% end %>

 <%= link_to "Show", book_path(@book.id) %>
 |
 <%= link_to "Back", books_path %>